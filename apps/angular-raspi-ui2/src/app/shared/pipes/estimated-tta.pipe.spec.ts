import { EstimatedTtaPipe } from './estimated-tta.pipe';

describe('EstimatedTtaPipe', () => {
  it('create an instance', () => {
    const pipe = new EstimatedTtaPipe();
    expect(pipe).toBeTruthy();
  });
});
